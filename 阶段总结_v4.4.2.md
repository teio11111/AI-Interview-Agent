# AI Interview Agent · 阶段总结

> 截至 v4.4.2 · 2026-08-06

---

## 一、需求

**业务目标**：构建一个 AI 驱动的面试辅助平台，覆盖「岗位解析 → 简历评估 → 智能出题 → 实时面试 → 决策报告」全闭环。

**核心用户场景**：
- HR：批量导入候选人、AI 自动评估、对比候选人、生成评估报告
- 面试官：实时面试、查看 AI 实时建议、结束面试生成 PDF 报告
- 候选人：投递简历、查看面试安排、回顾面试记录

**功能边界**：
- ✅ 多 agent 协作（出题 / 追问 / 评分 / 终评 / 简历分析 / 岗位匹配）
- ✅ SSE 流式推送（评估、出题、面试实时反馈）
- ✅ 实时面试 + ASR 说话人分离
- ✅ 多账号 + 操作审计 + 数据迁移
- ✅ PDF 决策报告导出
- 🚧 移动端演示 App（v5 即将启动）

---

## 二、技术实现

### 2.1 技术栈

| 层 | 选型 | 备注 |
|---|---|---|
| 后端 | Python 3.14 + Flask 3.1 + SQLAlchemy | 路由 / ORM |
| 实时通信 | Flask-SocketIO 5.6 + simple-websocket | ASR / 实时面试 |
| 流式响应 | SSE + waitress | LLM 流式输出（零缓冲） |
| 数据库 | MySQL 8 + PyMySQL | localhost:3307（本地）/ 3306（云端） |
| LLM | DeepSeek-V4-Flash（主）/ MiniMax-M3（备） | OpenAI 兼容协议 |
| ASR | 讯飞实时转写（roleType=2 角色分离） | WSS 长连接 |
| 鉴权 | Flask session + login_required | admin / admin123 |
| 前端 | Bootstrap 5 + 原生 JS + Socket.IO client | 单页 + 多页混合 |

### 2.2 多智能体协作架构

```
AgentOrchestrator
├── 岗位分析官（JD 解析）
├── 简历评估官（综合元评估）
├── 出题官组（3 并行：技能验证 / 经验深挖 / 软素质）
├── 选题官（合并 + 去重 + 过滤）
├── 追问官（实时面试中触发）
├── 评分官（每题评分）
└── 终评官（综合报告生成）
```

- LLM 调用全部走 `agents/` 模块的 `BaseAgent`
- 并行用 `concurrent.futures.ThreadPoolExecutor`，3 路并发
- 失败重试：`3s / 10s / 30s` 指数退避
- JSON 解析：`parse_json` 容错修复截断 / 多余逗号 / 单引号

### 2.3 关键模块

| 模块 | 文件 | 职责 |
|---|---|---|
| 路由层 | `routes/` (8) | HTTP 接口 / SSE / WebSocket |
| 服务层 | `services/` (6) | 业务逻辑封装 |
| 数据模型 | `models/` (7) | SQLAlchemy ORM |
| 仓储层 | `repositories/` (4) | 数据访问抽象 |
| 工具层 | `utils/` (8) | 鉴权 / 审计 / 响应包装 |
| Prompt 库 | `prompts/` | 各 agent 的 prompt 模板 |
| 前端模板 | `templates/` (8) | Bootstrap 页面 |

### 2.4 部署

- **本地**：`python app.py`，waitress 启动，端口 8088
- **云端**：`steve@10.32.2.219:8088`，Ubuntu，宝塔面板
- **CI**：手动 `deploy/cloud_update.sh` 增量更新（停服务 → 备份 → 解压 → 同步 → 启动）
- **备份目录**：`/home/steve/ai-interview_backup_<时间戳>/`

---

## 三、项目框架

```
AI-Interview-Agent/
├── app.py                # 应用工厂 + SocketIO 事件注册
├── constants.py          # 常量（SessionStatus 等）
├── extensions.py         # SQLAlchemy / SocketIO 初始化
├── config.py             # 配置加载（dev / prod）
├── agents/               # 多智能体实现
│   ├── base_agent.py
│   ├── job_analyzer.py
│   ├── resume_evaluator.py
│   ├── question_generator.py
│   └── ...
├── prompts/              # 各 agent 的 prompt 模板
├── routes/               # HTTP 路由
│   ├── auth_routes.py
│   ├── candidate_routes.py
│   ├── position_routes.py
│   ├── interview_routes.py
│   ├── stream_routes.py  # ★ SSE 流式（评估 + 出题）
│   ├── asr_routes.py     # ★ 讯飞 ASR 网关
│   └── ...
├── services/             # 业务逻辑
│   ├── interview_service.py  # ★ 核心
│   ├── asr_service.py
│   ├── llm_service.py
│   └── ...
├── models/               # ORM
├── repositories/         # 数据访问
├── utils/                # 工具
├── templates/            # Jinja2 模板
│   ├── base.html / index.html / login.html
│   ├── candidates.html / positions.html
│   ├── live_interview.html  # ★ 实时面试主页面
│   └── profile.html
├── static/               # 静态资源（JS / CSS）
│   └── socket.io.min.js  # 本地化（云端 CDN 不可达）
├── data/                 # 上传文件（简历 PDF）
├── logs/                 # 日志
├── fonts/                # PDF 中文字体
├── screenshots/          # 文档截图
├── smoke_test.py         # ★ 冒烟测试（111 项）
├── tests/                # 单元测试
└── deploy/               # 部署脚本
    ├── cloud_update.sh
    ├── install.sh / start.sh / stop.sh
    ├── requirements_full.txt
    └── UPLOAD_GUIDE.md
```

**代码规模**：约 8000+ 行 Python、3000+ 行 HTML/JS

---

## 四、重点问题与解决方法

### 问题 1：LLM 分析卡死（v3.x → v4.2）

**症状**：评估 / 出题阶段 SSE 推进到 75% 后卡住 5+ 秒才出结果

**根因**（三大）：
1. `parse_json` 无法容错 LLM 截断的 JSON
2. `concurrent.futures` `_run_parallel` 没设超时
3. `worker` 线程无法取消，LLM 失败后还在跑

**解决**：
- 强化 `parse_json`：尝试补全右括号 / 移除多余逗号 / 单引号转换
- `_run_parallel` 加全局超时 60s，强制 cancel 未完成 worker
- 加 `import time` 修复 v3.x 漏 import 的隐性 NameError

**commit**：`a7fd35b` v4.2 LLM 分析卡死三大根因根治

### 问题 2：实时面试 SSE 流卡死（v4.4.2）

**症状**：出题阶段前端 95s 看不到任何事件，UI 像卡死

**根因**：3 个出题官并行 LLM 调用，最慢的要 95s；后端只发 `: keepalive` 注释，前端 SSE parser 不识别

**解决**：
- `stream_question_generation` 主循环改为每 5s yield 一条 progress 事件
- 加总超时 360s（6 分钟），超时后主动 break + yield error
- 前端 UI 持续收到心跳，体验从"卡死"变成"等待中"

**commit**：`30fc1f1` v4.4.2 出题 SSE 增加心跳进度与总超时保护

**实测**：出题期间最大间隙从 95s → 5.1s

### 问题 3：兜底题功能导致真实问题被掩盖（v4.3）

**症状**：LLM 调用失败时系统自动塞"兜底题"，掩盖了真实 bug

**解决**：彻底删除兜底题逻辑，LLM 失败时直接报错给前端，让用户看到真实错误

**commit**：`f498629` v4.3 彻底删除兜底题功能

### 问题 4：候选人岗位错位（v4.2 → 前端岗位出题错位）

**症状**：前端选了"前端工程师"岗位，AI 出了 JAVA 相关的题

**根因**：
- Prompt 没硬约束岗位类型
- 没按生成岗位过滤已出题
- 重新评估时旧 session 没清理

**解决**：
- Prompt 硬约束：`如果岗位是 X，则严禁出 Y 方向的题`
- 代码层过滤：拿到生成结果后按 position_type 二次校验
- 重新评估时清理 candidate 的旧 session

**commit**：`a2b5a2e` 前端岗位出题错位根治

### 问题 5：云端 ASR 不显示结果（v4.0 修复）

**症状**：本地 ASR 正常，云端浏览器实时面试看不到识别文字

**根因**：
- `socket.io.min.js` 走 CDN，云端内网无法访问
- SocketIO `@socketio.on('join')` 事件处理器写在 `if __name__` 里，waitress `--call` 模式不执行

**解决**：
- socket.io.min.js 改本地化引入
- `@socketio.on('join'/'leave')` 提到模块顶层（app.py:84-91）

**commit**：`781598a` + `aadf22d` v4.0 修复

### 问题 6：隐性评估超时（v4.3.2）

**症状**：隐性维度分析完成后 `_hidden_done=False`，后续流程卡住

**解决**：在隐性评估 worker 完成时显式 set True，加超时熔断

**commit**：`5b16706` v4.3.2

### 问题 7：waitress 缓冲导致 SSE 不流畅（v3.1）

**症状**：前端进度条卡在 75%，要等 5s 心跳才 flush

**根因**：原 `socketio.run(app, debug=True)` 用 Werkzeug dev server，会缓冲 SSE 输出

**解决**：改用 `waitress.serve(app, host='0.0.0.0', port=8088, threads=8)`，每个 yield 立即送达

---

## 五、版本演进时间线

| 版本 | 核心交付 |
|---|---|
| v1.0 | AI Interview Agent 完整项目初始化 |
| v2.0/v2.1 | 评分逻辑修复 + 隐性维度异步评估 |
| v3.0/v3.1 | 多账号 + 改密 + 操作审计 + 数据迁移 admin1-5 |
| v4.0 | 线上面试双流音频 + AI 语义匹配 + 安全加固 |
| v4.1 | 演示前冲刺：配置硬化 + 单元测试 + 打包工具 |
| v4.2 | LLM 分析卡死三大根因根治 |
| v4.3 | 彻底删除兜底题 |
| v4.4 | 两段式拆分：评估自动完成 + 出题手动触发 |
| v4.4.1 | 出题完成后自动跳转面试工作台 |
| **v4.4.2** | **出题 SSE 心跳 + 总超时保护**（当前） |

**总提交**：15 个版本 commit

---

## 六、当前状态

- ✅ 云端服务稳定运行（PID 1984512，已运行 ~26h）
- ✅ 冒烟测试 111 PASS / 0 FAIL / 2 SKIP
- ✅ 端到端评估 81s + 出题 88s（带心跳，最大间隙 5.1s）
- ✅ 代码已推送到 `git.dgtis.com/dgtai/oai-interview.git`（master，30fc1f1）
- 🚧 移动端 App（xiaozhi-unity 方案 A）方案已选定，待启动

## 七、下一阶段

**移动端 AI 面试助手**（v5.0）：
- 基于 xiaozhi-unity（GitHub 开源）二次开发
- 方案 A：HTTP + JSON 替代原 WebSocket 协议
- 后端新增 3 个接口（拉题 / 对话 / 存结果）
- 预计 1 周交付安卓 APK
- 详情见 `PRD_移动端AI面试助手_简述版.docx`